const menuToggle=document.getElementById('menuToggle');const navLinks=document.getElementById('navLinks');menuToggle?.addEventListener('click',()=>{const isOpen=navLinks.classList.toggle('open');menuToggle.setAttribute('aria-expanded',String(isOpen))});document.querySelectorAll('.nav-links a').forEach(link=>link.addEventListener('click',()=>{navLinks.classList.remove('open');menuToggle?.setAttribute('aria-expanded','false')}));const revealObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');revealObserver.unobserve(entry.target)}})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>revealObserver.observe(el));const pricedInputs=['chairsQty','table6Qty','table8Qty','roundQty','tent10Qty'];function calculateEstimate(){let total=0;pricedInputs.forEach(id=>{const input=document.getElementById(id);total+=Number(input.value||0)*Number(input.dataset.price||0)});document.getElementById('estimateTotal').textContent=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(total)}document.querySelectorAll('input[type="number"]').forEach(input=>input.addEventListener('input',calculateEstimate));calculateEstimate();document.getElementById('bookingForm')?.addEventListener('submit',event=>{event.preventDefault();const getValue=id=>document.getElementById(id).value.trim();const quantity=id=>Number(document.getElementById(id).value||0);const name=getValue('customerName'),phone=getValue('customerPhone'),email=getValue('customerEmail'),date=getValue('rentalDate'),fulfillment=getValue('fulfillment'),location=getValue('eventLocation'),paymentMethod=getValue('paymentMethod'),paymentTiming=getValue('paymentTiming'),notes=getValue('bookingNotes'),estimate=document.getElementById('estimateTotal').textContent;const items=[['Chairs',quantity('chairsQty')],['6-ft tables',quantity('table6Qty')],['8-ft tables',quantity('table8Qty')],['Round tables',quantity('roundQty')],['10x10 tents',quantity('tent10Qty')],['20x20 tents',quantity('tent20Qty')]].filter(([,qty])=>qty>0).map(([item,qty])=>`${item}: ${qty}`).join('\n');if(!items&&!notes){alert('Please select at least one rental item or describe your request.');return}const subject=encodeURIComponent(`Rental Request - ${date||'Date Needed'} - ${name}`);const body=encodeURIComponent(`Hello Legacy Path Rentals,\n\nI would like to request a rental reservation.\n\nCUSTOMER\nName: ${name}\nPhone: ${phone}\nEmail: ${email||'Not provided'}\n\nRENTAL DETAILS\nDate: ${date}\nFulfillment: ${fulfillment}\nEvent location: ${location||'Not provided'}\n\nITEMS\n${items||'See notes below'}\n\nEstimated equipment subtotal: ${estimate}\nPreferred payment method: ${paymentMethod}\nPayment preference: ${paymentTiming}\n\nADDITIONAL DETAILS\n${notes||'None'}\n\nI understand that availability, delivery, setup, deposits, taxes, and final pricing must be confirmed by Legacy Path Rentals.`);window.location.href=`mailto:info@legacypathrentals.com?subject=${subject}&body=${body}`});
